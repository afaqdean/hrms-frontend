import AdminProfileSettings from '@/containers/admin/profile-settings/AdminProfileSettings';
import React from 'react';

const page = () => {
  return (
    <AdminProfileSettings />
  );
};

export default page;
