import ResetPasswordContainer from '@/containers/shared/reset-password/ResetPasswordContainer';

import React from 'react';

const ResetPassword = () => {
  return (
    <ResetPasswordContainer />
  );
};

export default ResetPassword;
